import React from 'react';

const handleLeftClick = (e: React.MouseEvent): boolean => {
  e.preventDefault();
  return false;
}

const handleRightClick = (e: React.MouseEvent): boolean => {
  e.preventDefault();
  return false;
}
